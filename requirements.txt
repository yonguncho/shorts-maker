# Trader Cho — Python 의존성
# 설치: .venv/bin/pip install -r requirements.txt

# ── 데이터 수집 ─────────────────────────────────────────────────────────────
yfinance>=1.4.0
pandas>=2.0.0
requests>=2.30.0
feedparser>=6.0.0
beautifulsoup4>=4.12.0
lxml>=5.0.0

# ── AI / LLM ────────────────────────────────────────────────────────────────
anthropic>=0.29.0

# ── 이미지 / 영상 ────────────────────────────────────────────────────────────
Pillow>=10.0.0
matplotlib>=3.8.0
numpy>=1.26.0

# ── 스케줄링 ────────────────────────────────────────────────────────────────
schedule>=1.2.0
pytz>=2024.1
pandas_market_calendars>=5.0.0

# ── 멀티소스 (P2~P4) ─────────────────────────────────────────────────────────
pytrends>=4.9.0
praw>=7.8.0           # Reddit — OAuth 앱 등록 필요(미보유 시 graceful skip)

# ── 기타 ────────────────────────────────────────────────────────────────────
python-dotenv>=1.0.0
